"use client";
import { usePathname } from 'next/navigation';
import React, { use } from 'react';

const page = () => {
    return (
        <div className='h-[400px] flex justify-center items-center text-3xl font-bold'>
            this login page
        </div>
    );
};

export default page;