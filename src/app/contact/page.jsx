import React from 'react';


export const metadata = {
        title: "Contact Page",
        description: "This is contact page"
    };

const page = () => {
    
    return (
        <div>
            this is contact page
        </div>
    );
};

export default page;