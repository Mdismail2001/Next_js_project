import React from 'react';

export const metadata = {
        title: "About Page",
        description: "This is about page"
    };  

const page = () => {
    return (
        <div>
            this is about page
        </div>
    );
};

export default page;